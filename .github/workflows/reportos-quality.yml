name: ReportOS Quality

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: reportos-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      - name: Next build
        run: npm run build

      - name: Cloudflare Worker build
        run: npm run build:worker

      # Keep this blocking, but run it after application validation so an
      # upstream transitive advisory cannot hide independent code/build regressions.
      - name: Production dependency audit
        run: npm audit --omit=dev --audit-level=high
